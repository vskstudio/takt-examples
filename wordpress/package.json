{
  "name": "takt-example-wordpress",
  "private": true,
  "description": "Takt WordPress plugin example: WordPress + WooCommerce via wp-env, verified end-to-end with Playwright against a mocked Takt ingest.",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:clean": "wp-env destroy",
    "setup": "wp-env run cli wp eval-file wp-content/takt-e2e/setup-wp.php",
    "e2e": "wp-env start && npm run setup && playwright test",
    "e2e:run": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@wordpress/env": "^10.10.0"
  }
}
